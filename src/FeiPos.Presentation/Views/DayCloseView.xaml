<UserControl x:Class="FeiPos.Presentation.Views.DayCloseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Finalizar día" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12"/>
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <UniformGrid Columns="2">
                <TextBlock Text="{Binding SalesTotal, StringFormat=Ventas: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding CashTotal, StringFormat=Efectivo: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding CardTotal, StringFormat=Tarjeta: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding CheckTotal, StringFormat=Cheque: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding CreditTotal, StringFormat=Crédito: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding ExpectedCash, StringFormat=Efectivo esperado: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding DepositsTotal, StringFormat=Depósitos: ₡{0:N2}}" Margin="0,0,12,8"/>
                <TextBlock Text="{Binding WithdrawalsTotal, StringFormat=Retiros: ₡{0:N2}}" Margin="0,0,12,8"/>
            </UniformGrid>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Efectivo contado"/>
                <TextBox Text="{Binding CountedCash, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
                <TextBlock Text="{Binding Difference, StringFormat=Diferencia: ₡{0:N2}}" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Actualizar" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
                    <Button Content="Finalizar día" Command="{Binding CloseDayCommand}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Closures}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding BusinessDate, StringFormat=d}" Width="120"/>
                <DataGridTextColumn Header="Cerrado" Binding="{Binding ClosedAt, StringFormat=g}" Width="150"/>
                <DataGridTextColumn Header="Ventas" Binding="{Binding SalesTotal, StringFormat=₡{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Esperado" Binding="{Binding ExpectedCash, StringFormat=₡{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Contado" Binding="{Binding CountedCash, StringFormat=₡{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Diferencia" Binding="{Binding Difference, StringFormat=₡{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Notas" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
