<UserControl x:Class="FeiPos.Presentation.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019">
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Gestión de Clientes" Style="{StaticResource TitleTextBlockStyle}" />
                <ui:AppBarButton Label="Nuevo Cliente" Icon="Add" Margin="20,0,0,0" Command="{Binding AddCustomerCommand}"/>
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Customers}" 
                      SelectedItem="{Binding SelectedCustomer}"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Identificación" Binding="{Binding Identification}" Width="150"/>
                    <DataGridTextColumn Header="Nombre Completo" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Panel de Edición -->
        <Border Grid.Column="1" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}" 
                CornerRadius="8" Padding="15" Visibility="{Binding SelectedCustomer, Converter={StaticResource NullToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Ficha de Cliente" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,15"/>
                
                <TextBlock Text="Nombre / Razón Social" Margin="0,10,0,5"/>
                <TextBox Text="{Binding SelectedCustomer.FullName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Cédula (Física/Jurídica/DIMEX)" Margin="0,10,0,5"/>
                <TextBox Text="{Binding SelectedCustomer.Identification, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Correo Electrónico (para XML)" Margin="0,10,0,5"/>
                <TextBox Text="{Binding SelectedCustomer.Email, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Teléfono" Margin="0,10,0,5"/>
                <TextBox Text="{Binding SelectedCustomer.PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="Guardar Cliente" Style="{StaticResource AccentButtonStyle}" 
                        HorizontalAlignment="Stretch" Margin="0,30,0,0"
                        Command="{Binding SaveChangesCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
