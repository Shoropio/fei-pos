<UserControl x:Class="FeiPos.Presentation.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style x:Key="DetailColumnStyle" TargetType="ColumnDefinition">
                <Setter Property="Width" Value="0"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding SelectedCustomer}" Value="{x:Null}">
                        <Setter Property="Width" Value="0"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SelectedCustomer, Converter={StaticResource NullToVisibilityConverter}}" Value="Visible">
                        <Setter Property="Width" Value="380"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Style="{StaticResource DetailColumnStyle}" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Gestion de Clientes"
                           Style="{StaticResource TitleTextBlockStyle}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left" />
                <ui:AppBarButton Label="Nuevo Cliente"
                                 Grid.Column="1"
                                 Icon="Add"
                                 HorizontalAlignment="Right"
                                 Command="{Binding AddCustomerCommand}" />
            </Grid>

            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Customers}"
                      SelectedItem="{Binding SelectedCustomer}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Identificacion" Binding="{Binding Identification}" Width="160"/>
                    <DataGridTextColumn Header="Nombre Completo" Binding="{Binding FullName}" Width="2*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Column="1"
                Background="{StaticResource AppPanelBrush}"
                BorderBrush="{StaticResource AppBorderBrush}"
                BorderThickness="1"
                CornerRadius="0"
                Padding="15"
                Visibility="{Binding SelectedCustomer, Converter={StaticResource NullToVisibilityConverter}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock Text="Ficha de Cliente"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,15"/>

                    <TextBlock Text="Nombre / Razon Social" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding SelectedCustomer.FullName, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Cedula (Fisica/Juridica/DIMEX)" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding SelectedCustomer.Identification, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Correo Electronico (para XML)" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding SelectedCustomer.Email, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Telefono" Margin="0,10,0,5"/>
                    <TextBox Text="{Binding SelectedCustomer.PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                    <Button Content="Guardar Cliente"
                            Style="{StaticResource AccentButtonStyle}"
                            HorizontalAlignment="Stretch"
                            Margin="0,30,0,0"
                            Command="{Binding SaveChangesCommand}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="1"
                   Grid.ColumnSpan="2"
                   Text="{Binding StatusMessage}"
                   Foreground="{StaticResource AppMutedTextBrush}"
                   Margin="0,10,0,0"/>
    </Grid>
</UserControl>
